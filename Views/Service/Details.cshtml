@model Obeli_K.Models.Service

@{
    ViewData["Title"] = "Détails du Service";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <div class="card shadow-sm">
                <div class="card-header bg-info text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-diagram-3"></i> Détails du Service
                    </h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <dl class="row">
                                <dt class="col-sm-4">Nom :</dt>
                                <dd class="col-sm-8"><strong>@Model.Nom</strong></dd>

                                <dt class="col-sm-4">Code :</dt>
                                <dd class="col-sm-8">
                                    @if (!string.IsNullOrEmpty(Model.Code))
                                    {
                                        <span class="badge bg-secondary">@Model.Code</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </dd>

                                <dt class="col-sm-4">Direction :</dt>
                                <dd class="col-sm-8">
                                    @if (Model.Direction != null)
                                    {
                                        <a href="@Url.Action("Details", "Direction", new { id = Model.DirectionId })">
                                            <span class="badge bg-primary">@Model.Direction.Nom</span>
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </dd>

                                <dt class="col-sm-4">Description :</dt>
                                <dd class="col-sm-8">@(Model.Description ?? "-")</dd>
                            </dl>
                        </div>

                        <div class="col-md-6">
                            <dl class="row">
                                <dt class="col-sm-4">Responsable :</dt>
                                <dd class="col-sm-8">@(Model.Responsable ?? "-")</dd>

                                <dt class="col-sm-4">Email :</dt>
                                <dd class="col-sm-8">
                                    @if (!string.IsNullOrEmpty(Model.Email))
                                    {
                                        <a href="mailto:@Model.Email">@Model.Email</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </dd>
                            </dl>
                        </div>
                    </div>

                    <hr />

                    <div class="row">
                        <div class="col-md-12">
                            <h5><i class="bi bi-people"></i> Utilisateurs de ce Service</h5>
                            
                            @if (ViewBag.Utilisateurs != null && ((IEnumerable<dynamic>)ViewBag.Utilisateurs).Any())
                            {
                                <div class="table-responsive mt-3">
                                    <table class="table table-sm table-hover">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Nom</th>
                                                <th>Matricule</th>
                                                <th>Email</th>
                                                <th>Téléphone</th>
                                                <th>Fonction</th>
                                                <th>Direction</th>
                                                <th>Rôle</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var user in ViewBag.Utilisateurs)
                                            {
                                                <tr>
                                                    <td>
                                                        <strong>@user.Nom @user.Prenoms</strong>
                                                    </td>
                                                    <td>@user.UserName</td>
                                                    <td>
                                                        @if (!string.IsNullOrEmpty(user.Email))
                                                        {
                                                            <a href="mailto:@user.Email">@user.Email</a>
                                                        }
                                                        else
                                                        {
                                                            <span class="text-muted">-</span>
                                                        }
                                                    </td>
                                                    <td>@(user.PhoneNumber ?? "-")</td>
                                                    <td>@user.FonctionNom</td>
                                                    <td>@user.DirectionNom</td>
                                                    <td><span class="badge bg-secondary">@user.Role</span></td>
                                                    <td>
                                                        <a href="@Url.Action("Details", "Utilisateur", new { id = user.Id })" 
                                                           class="btn btn-sm btn-info">
                                                            <i class="bi bi-eye"></i>
                                                        </a>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            }
                            else
                            {
                                <div class="alert alert-info mt-3" role="alert">
                                    <i class="bi bi-info-circle"></i> Aucun utilisateur dans ce service.
                                </div>
                            }
                        </div>
                    </div>

                    <hr />

                    <div class="row">
                        <div class="col-md-6">
                            <small class="text-muted">
                                <strong>Créé le :</strong> 
                                @if (Model.CreatedOn != null)
                                {
                                    @Model.CreatedOn.Value.ToString("dd/MM/yyyy HH:mm")
                                }
                                @if (!string.IsNullOrEmpty(Model.CreatedBy))
                                {
                                    <span>par @Model.CreatedBy</span>
                                }
                            </small>
                        </div>
                        <div class="col-md-6">
                            @if (Model.ModifiedOn != null)
                            {
                                <small class="text-muted">
                                    <strong>Modifié le :</strong> @Model.ModifiedOn.Value.ToString("dd/MM/yyyy HH:mm")
                                    @if (!string.IsNullOrEmpty(Model.ModifiedBy))
                                    {
                                        <span>par @Model.ModifiedBy</span>
                                    }
                                </small>
                            }
                        </div>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <a href="@Url.Action("List", "Service")" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Retour à la liste
                        </a>
                        <div>
                            <a href="@Url.Action("Edit", "Service", new { id = Model.Id })" class="btn btn-warning">
                                <i class="bi bi-pencil"></i> Modifier
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
